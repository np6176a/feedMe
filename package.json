{
  "name": "feed-me-express",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.16.2",
    "node-fetch": "^2.2.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
